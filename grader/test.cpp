#include <iostream>
#define N 500
using namespace std;

int p,k,a,b,x;
vector<int> v(N), qs(N);

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> p >> k >> a >> b;
    cout << 1;
}